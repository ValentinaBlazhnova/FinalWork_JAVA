public enum Commands {
    ADD, //Добавляет новые игрушки для розыгрыша
    LOTTERY, // Случайно выбирает игрушку из списка и записывает игрушку в файл.
    PRIZES, //Cчитывает из файла строки с призовыми игрушками, переводит их в экземпляры класса Toy и выводит в консоль.
    EXIT // выход
}
